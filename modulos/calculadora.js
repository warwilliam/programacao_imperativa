
function calcular(tempoDigitado,comida,)
{
    switch (comida)
    {
        case `pipoca`:
        (tempoDigitado > 20 && tempoDigitado >=30)
        break;
        case 'macarrao': console,log(operacoes.subtracao(a,b))
        break;
        case `carne`: console.log(operacoes.multiplicacao(a,b))
        break;
        case 'feijao': console.log(operacoes.divisao(a,b))
        break;
        case `brigadeiro`: console.log(tabuada(a))
        break
    }

}

calcular(6,5,'+')


1 - Pipoca – 10 segundos (padrão);
2 - Macarrão – 8 segundos (padrão);
3 - Carne – 15 segundos (padrão);
4 - Feijão – 12 segundos (padrão);
5 - Brigadeiro – 8 segundos (padrão); 

- O usuário poderá alterar o tempo padrão, aumentando ou diminuindo de acordo com sua vontade. Se o tempo informado for maior que 2x o necessário, exibir mensagem que a comida queimou.
- Se o tempo for menor que o padrão, exibir a mensagem: "tempo insuficiente"; 
- Opções não listadas no menu, devem exibir uma mensagem de erro: "Prato inexistente";
- Se o tempo for 3x maior que o necessário para o prato, o microondas deve exibir a mensagem: “kabumm”;
- No final de cada tarefa, o microondas deverá exibir a mensagem: "Prato pronto, bom apetite!!!".