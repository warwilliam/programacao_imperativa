/* Em um arquivo diferente, crie o objeto literal curso que tem como atributos:
 nome do curso (string), 
 nota de aprovação (number), 
 faltas máximas (number) 
 e uma lista de estudantes (um array composto pelos alunos criados no passo 1). */

 let curso={
     nome:'CTD',
     notaAprovacao:7,
     faltasMaximas:4,
     listaEstudantes:['Patrick','Sara','Matheus','William','Patricia']
 }

 const aluno=require('./parte1e2')

 const listaAluno=new aluno('Patrick',7[9,10,8,9])
